import { createParamDecorator, ExecutionContext } from '@nestjs/common';

/*
export const KafkaPayload = createParamDecorator((data: string, ctx: ExecutionContext) => {
  const request = ctx.switchToRpc().getContext();
  return request.getMessage().value;
});
*/
